services:
  core:
    image: scafoldr/core
    build:
      context: ./core
    restart: unless-stopped
    
  web:
    image: scafoldr/web
    build:
      context: ./web
    restart: unless-stopped

  proxy:
    image: scafoldr/proxy
    build:
      context: ./proxy
    restart: unless-stopped
