import { {{ entity_name }} } from '@/drizzle/schema';
import { db } from '@/lib/db';
import { createRESTAPI } from '@/lib/rest-generator';

const api = createRESTAPI({{ entity_name }}, db, '{{ entity_name }}');
export const { GET, POST, PUT, DELETE } = api;